# v0.6.0 Phase 9 — audit + merge + push

Branch `v0.6.0-feature-stack` has 9 phase commits, working tree clean, no halt notes. Ready to ship pending audit.

## Audit checklist (run all, all must pass)

1. `git status` — confirm clean
2. `git log v0.6.0-feature-stack ^master --oneline` — confirm 9 commits present
3. `cat CHANGELOG.md | head -30` — confirm v0.6.0 entry accurate, lists all phase additions
4. `bash -n hooks/pre-compact && bash -n hooks/session-start && bash -n hooks/stop-mark-memory && bash -n hooks/userprompt-context-warn` — all bash hooks parse
5. `jq empty < hooks/hooks.json && jq empty < .claude-plugin/plugin.json` — JSON valid
6. `cat .claude-plugin/plugin.json | grep version` — confirms `0.6.0`
7. `ls .github/workflows/` — confirm `lint.yml` exists
8. `ls skills/scribe-status skills/xref-lint` — confirm new skill dirs exist with `SKILL.md` inside

## On all green, merge + push

9. `git checkout master`
10. `git merge --no-ff v0.6.0-feature-stack -m "merge: v0.6.0 feature stack"`
11. `git push origin master`
12. `git log master --oneline -3` — confirm merge commit on top, v0.5.0 underneath

## On any audit failure

Halt per protocol:

- Write halt note to `docs/plans/v0.6.0-halt-9.md` using the halt-note template in `docs/plans/v0.6.0-autonomous-rollout.md`
- Do NOT merge
- Do NOT reset the feature branch (keep all work intact for debugging)
- Stop and report

## After successful push, report

- Merge commit SHA
- Confirmation v0.6.0 is live on `origin/master`
- Reminder that Phase 10 (HITL smoke test) is owed by user on next real session

## Do NOT do

- Do NOT update local plugin cache (`~/.claude/plugins/cache/project-scribe/...`)
- Do NOT modify `~/.claude/plugins/installed_plugins.json`
- Both deferred to user, post-merge
