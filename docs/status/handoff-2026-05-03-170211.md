# Session Handoff — 2026-05-03 17:02:11

**Branch:** master
**Phase:** v0.7.5 spec + plan complete, ready to execute
**Mode:** quick (doc only)
**Previous session ended at:** `27c4afd` (clean tree, pushed to origin/master)

---

## One-paragraph summary

Brainstormed, spec'd, planned, and reviewed v0.7.5 — `/project-scribe:context-audit`, a read-only diagnostic for CLAUDE.md (project + global) and MCP server token bloat. Backlog item #3 from research synthesis (Brad signal 8/10). Spec committed at `8396332`, plan at `27c4afd`. Plan is 1986 lines across 6 chunks with ~22 bats cases. Per-chunk reviewer loop ran two iterations on chunks 2/3/5 to resolve substantive blockers (critical-fixture math, deferred exit-1, sha_quote shell-injection, within-file dup suppression, real line numbers, SKILL.md frontmatter convention, top-consumers section ordering, `cut` vs parameter expansion for newline-safe field splitting). All chunks now ✅ approved. **Next session: execute plan via subagent-driven-development.**

---

## Just decided (this session)

- **Context Audit scope = CLAUDE.md (project + global) + MCP servers.** Hooks/skills audit deferred to v0.7.6. Char/4 token heuristic (matches `compact-decisions` precedent, ±15% accuracy footnoted). Hardcoded 5k/15k severity thresholds. Three finding types: size, duplication (≥200-char paragraph-block hash, cross-file only), staleness (date markers >6mo + dead refs). MCP audit covers project + OS-specific global config with curated lookup table + honest unknown fallback.
- **Architecture: single bash script.** Skill + helper script (`scripts/context-audit.sh`) matches v0.7.4 `scripts/` precedent. Rejected multi-script split (`scripts/lib/`) as speculative invention before second feature needs it.
- **Tag-after-merge ordering.** v0.7.5 plan tags AFTER squash-merge lands on master, not before — pre-merge tag points at orphaned SHA after squash.
- **STATE.md `## Last shipped` is reconcile-skill-owned.** Plan no longer manually pre-populates SHAs — would be wiped on next session start anyway and risks pointing at orphaned pre-squash commits.

(Decisions log entry will be written during execution at Task 6.1, not yet committed.)

---

## Active work

**In progress:** none — spec + plan shipped this session, both pushed to origin.

**Next concrete step:**
Execute `docs/superpowers/plans/2026-05-03-context-audit.md` via `superpowers:subagent-driven-development`. Recommended worktree: `.worktrees/feature/context-audit` (matches v0.7.4 pattern).

**Pending todos (backlog order after v0.7.5 ships):**
1. **#4 Token-budget tab in session-card** (Brad signal 8/10) — adds 5-hour window remaining + weekly cap projection to session-card output.
2. **#5 Plugin "requires" compat field** (research Anthropic schema first).
3. **#6 Plan-as-GitHub-issue** (Matt Pocock signal 7/10).

Then L1+ items per `docs/superpowers/plans/2026-04-30-bash-test-harness.md` backlog.

---

## Files touched this session

| Path | Status | Why |
|---|---|---|
| `docs/superpowers/specs/2026-05-03-context-audit-design.md` | new (`8396332`) | v0.7.5 design spec — Brad backlog #3 |
| `docs/superpowers/plans/2026-05-03-context-audit.md` | new (`27c4afd`) | v0.7.5 implementation plan — 1986 lines, 6 chunks |
| `docs/STATE.md` | modified (uncommitted) | Refreshed Current focus + Last shipped + Specs/Plans tables for v0.7.5 |

---

## Uncommitted changes

`docs/STATE.md` — Current focus / Last shipped / Specs / Plans updated for v0.7.5 readiness. Not committed because reconcile-project-state will refresh Last shipped at next session start anyway. Decision: leave dirty so user sees the state-update intent on resume; or commit before close. **Recommend: commit on resume after reconcile runs.**

---

## Plan structure (for next-session quick reference)

| Chunk | Tasks | Output |
|---|---|---|
| 1: Skeleton + target resolution | 1.1, 1.2, 1.3 | `scripts/context-audit.sh` skeleton, flag parsing, OSTYPE detection (4 branches), CLAUDE.md target resolution, MCP config path resolution |
| 2: Token counting + size classification | 2.1 | char/4 heuristic, 5k/15k severity, 5 boundary tests (4999/5000/14999/15000/~16k fixture), `HAS_CRITICAL` deferred-exit pattern |
| 3: Duplication detection | 3.1 | Cross-file paragraph hashing via bash temp-file (NOT awk-internal), within-file dup suppression, ≥200-char threshold, 199/201 boundary tests |
| 4: Staleness detection | 4.1, 4.2 | Date markers >6mo with `SCRIBE_AUDIT_TODAY` env override, dead-ref scan, http-mangled local-path catch, future-date detection |
| 5: MCP audit + report polish + skill | 5.1, 5.2, 5.3, 5.4 | `scripts/mcp-token-estimates.json` (~30 entries), `audit_mcp_config` with jq, complete refactored `main()` with phases 1-10, SKILL.md (no `type:` field), slash command shim, $HOME-unset skip-with-TODO |
| 6: Release packaging | 6.1, 6.2, 6.3, 6.4 | Dogfood, DECISIONS entry, CHANGELOG/plugin.json/README bumps, STATE refresh (Current focus only), PR + squash-merge + tag-after-merge |

Definition of done: 11 items locked at end of plan.

---

## Open questions / decisions deferred

- **Worktree branch name:** plan suggests `feature/context-audit`. Confirm at execution start.
- **CI gating on exit code 1:** plan ships exit-1 on critical findings. User can optionally add this to CI as quality gate later — not in v0.7.5 scope.
- **`--mcp-only` flag:** deferred to v0.7.6 if demand surfaces.
- **MCP estimate registry maintenance:** initial table is best-effort + PR invitation. Long-term scrape strategy deferred.

---

## Paste-this prompt for the new session

```
Resume the project-scribe session handed off at 2026-05-03 17:02:11. Read the handoff at docs/status/handoff-2026-05-03-170211.md for context.

v0.7.5 spec + plan committed and pushed:
- Spec: docs/superpowers/specs/2026-05-03-context-audit-design.md (8396332)
- Plan: docs/superpowers/plans/2026-05-03-context-audit.md (27c4afd)

Plan reviewer-approved across all 6 chunks (two iterations on chunks 2/3/5 to resolve substantive blockers). Ready to execute.

Next step: Execute via superpowers:subagent-driven-development. Recommended worktree: .worktrees/feature/context-audit. First chunk dispatches scripts/context-audit.sh skeleton + target resolution.

Before starting:
1. Read docs/STATE.md (scribe auto-reconciles on startup — note STATE.md may have uncommitted v0.7.5 readiness edits worth committing after reconcile runs)
2. Read the handoff file in full
3. Confirm worktree branch name + execution preferences before dispatching first chunk

Working repo: C:/Users/forty/.claude/plugins/marketplaces/project-scribe (marketplace clone — real work lives here).
```

---

## What NOT to lose

1. **Caveman mode active** — full intensity. Code/commits/PRs still written normal. "stop caveman" / "normal mode" disables.
2. **No-shortcuts policy:** when picking architectural options, user does NOT want minimal-effort default. Locked patterns from this session: bash temp-file hashing (NOT awk-internal), parameter expansion (NOT cut) for tab-split with newline-bearing fields, deferred exit-1 (NOT inline truncate).
3. **Honesty contract** carried from v0.7.4: ambiguous = ⚠️ ambiguous abort, never silent guess. Applied to MCP unknown servers, OS-fallback warnings, malformed JSON exit code.
4. **`scripts/` precedent locked:** SKILL.md thin wrapper + `scripts/<name>.sh` heavy lifter + `tests/scripts/<name>.bats` + `tests/skills/<name>.bats`. v0.7.5 reinforces.
5. **Per-chunk reviewer loop pattern works:** dispatched all 6 reviewers in parallel first pass, then 3 re-reviews after fixes. Calibrated for this codebase.
6. **SKILL.md frontmatter convention is `name:` + `description:` only.** No `type:` field. Verified against scribe-verify, scribe-status, xref-lint, base-audit. Easy to violate by inheriting from spec drafts — caught and fixed in chunk 5 review.
7. **Squash-merge tag ordering:** tag the merge-commit SHA on master AFTER pull, not the pre-squash local commit.
8. **`SCRIBE_AUDIT_TODAY` env override pattern:** matches `SCRIBE_VERIFY_TIMEOUT` from v0.7.4. Date-based fixtures pin runtime "today" so tests don't rot. Carry forward to any future date-sensitive features.
9. **Two open follow-ups still alive from v0.7.4:** scribe-verify green-path test fixture (3 candidate approaches in skip message), stop-mark-memory empty-stdin skip patch. Not blockers, low-priority.
10. **Working repo location:** marketplace clone at `C:/Users/forty/.claude/plugins/marketplaces/project-scribe`. Downloads clone at `C:/Users/forty/Downloads/project-scribe` is stale (last seen at v0.7.2) — do NOT operate from there.
