# {{project_name}} — Decisions

Append-only log of project-wide decisions. Newest on top. Five fields each (Context, Decision, Alternatives considered, Revisit when). Never edit past entries — reverses are new entries linking back.

The "Alternatives considered" field captures rejected options + why. Defaults to "None recorded — add when relevant." for entries where alternatives weren't considered or are not worth surfacing. Old four-field entries remain valid; the field is optional on legacy entries.

---

_No decisions logged yet. First decision will appear above this line._
